# Phase 2：素材与预览任务清单

## 0. 变更治理

- [x] 创建活跃 Spec、实施计划和任务清单
- [x] 复核本 Spec 与展站 PRD、项目计划及 ADR-0001/0002 的范围一致性
- [ ] 每完成一个可验收子项，更新本任务清单和相关稳定文档

## 1. 素材清单与批准

- [x] 新建金源素材清单，逐项记录 Logo、产品图、证书图、产品 PDF、微信二维码和厂房图的用途、规格、来源、授权/确认、复核人、状态和 Asset ID
- [ ] 向客户/运营收集并核验可公开使用的真实 Logo
- [ ] 向客户/运营收集产品图，并建立产品型号到图片的映射
- [ ] 向客户/运营收集证书原件，核验展示许可、名称及有效性
- [ ] 向客户/运营收集可公开下载的产品 PDF，并核验内容可读取
- [ ] 向客户/运营收集微信二维码，并实际扫码确认目标账号
- [ ] 向客户/运营收集厂房图，并核验拍摄场所、展示许可及图片真实性
- [ ] 对每个缺失素材取得批准占位记录；制作中性、不可冒充真实客户素材的占位资源
- [x] 在金源文档中如实标注真实、待确认和批准占位的状态

## 2. OSS 与基础设施前置条件

- [ ] 确认 OSS bucket、地域、CORS、加密、生命周期和最小权限服务账号
- [x] 定义并实现服务端生成的临时上传、已复核 Asset 与 artifact 对象键策略
- [x] 配置上传签名所需的服务端环境变量；确保长期密钥不进入浏览器、日志或响应
- [ ] 确认平台 preview 域名、泛域名 DNS、`*.preview.{platformDomain}` HTTPS 证书和 OSS/CDN 绑定
- [ ] 记录中国大陆部署所需的平台域名 ICP/地域合规状态；未就绪时配置清晰的阻塞错误
- [x] 为开发和自动化测试提供不依赖生产 bucket/域名的受控替代配置

## 3. 数据库、仓库与服务端校验

- [x] 在 Drizzle schema 中新增 `assets`、`build_artifacts`、`deployments` 及必要索引、外键和时间字段
- [x] 编写追加式 Phase 2 MySQL migration 和对应回滚/运维说明
- [x] 扩展仓库接口及内存实现，以支持 Asset、artifact 和 deployment 生命周期
- [x] 修正创建站点事务：接收并校验初始配置，同时创建 `Site`、revision 1 和审计记录
- [x] 实现上传意图校验与短时、限定范围的 OSS 直传签名接口
- [x] 实现上传完成复核：对象存在性、实际 MIME/文件特征、体积、checksum、对象键和 `siteId` 归属；并以原子 Asset 创建处理并发完成、清理临时对象
- [x] 实现 `customer_provided | placeholder` 来源、占位批准信息及其数据库一致性约束；不增加重复的 `is_placeholder`
- [x] 建立覆盖 Logo、二维码、PDF、证书、产品图和厂房图的共享“配置路径 → 允许 Asset 类型”映射
- [x] 在创建草稿 Revision 时允许不存在的预留 Asset ID，但拒绝已存在且跨站或类型不匹配的 Asset
- [x] 在创建 Deployment 前要求全部 Asset 存在、已复核、归属同站、类型匹配且占位项已批准
- [x] 为上传、素材复核、artifact 和部署操作写入审计记录
- [x] 定义稳定的 Asset/部署错误码，避免向客户端泄露对象存储内部细节

## 4. 构建产物与 HTTPS 预览

- [x] 定义 `revision + templateVersion` 的构建输入、artifact ID 与不可变产物目录，并在对象写入前以数据库唯一记录和租约原子预留
- [x] 实现构建任务创建与状态查询 API
- [x] 实现本地/测试异步构建运行器与适配器：读取已验证 Revision 和 Asset、构建模板并写入 artifact
- [x] 接入持久化轮询 Worker：API 返回后由 Worker 原子领取任务；重启后可领取过期租约；提供独立 Worker 进程入口并允许 API 关闭内嵌消费者
- [x] 实现 preview 部署适配器，返回真实 HTTPS URL 或明确的基础设施配置错误
- [x] 实现部署幂等键，确保同一 artifact 内容不被覆盖，重复请求不产生重复有效任务
- [x] 将部署状态、artifact、URL、占位 Asset 清单和安全错误摘要持久化
- [x] 实现 HTTPS 首页、产品/资质/关于/联系路由及关键静态资源健康检查；仅检查通过后标记 `healthy`
- [ ] 验证标准配置连续 20 次部署的 P95 不超过 10 分钟
- [ ] 验证 API 不可用时已部署的纯静态 artifact 仍可浏览
- [ ] 在 iOS 16+ 与 Android 12+ 微信内置浏览器真机检查预览打开、拨号和 PDF 下载
- [ ] 在受控环境运行一次真实 HTTPS 预览 smoke test，并记录 Revision、artifact 和 URL

## 5. 后台与交互测试

- [x] 实现素材面板：选择 Asset 类型、上传、进度、复核状态、失败原因、真实/占位和批准状态
- [x] 实现 Revision 历史：显示当前编辑基线、创建者、时间和加载历史版本
- [x] 实现 `409 revision_conflict` 提示，展示服务器当前版本并提供重新加载最新版本操作
- [x] 确保重新加载不会静默丢弃本地未保存 JSON；必要时要求用户确认
- [x] 实现预览面板：选择 Revision、触发部署、查询状态、复制 HTTPS URL 与占位警告
- [x] 增加后台交互测试：历史加载、冲突、重新加载、保存恢复及部署状态显示

## 6. MySQL 集成与质量验证

- [x] 配置隔离的 `DATABASE_URL_TEST`、测试建库/清理及 migration 执行流程
- [x] 增加 AC-01～AC-03 回归测试：站点创建产生 revision 1 与审计、配置错误字段定位、并发保存冲突
- [x] 增加 MySQL 集成测试：Asset 外键、站点归属、Revision 乐观锁冲突、审计记录和 Phase 2 查询
- [x] 增加 API 测试：签名约束、完成复核、草稿预留 ID、跨站/类型不匹配、部署时未复核/未批准占位拒绝
- [x] 增加构建/部署测试：artifact 不可变性、幂等键、健康检查、状态查询和无 HTTPS 前置条件时的明确失败
- [x] 运行 `pnpm check`
- [x] 运行 `pnpm test`（由 `pnpm check` 执行）
- [x] 运行 `pnpm build`
- [ ] 在具备受控云凭据时运行 HTTPS 预览 smoke test

## 7. 文档回写与归档

- [x] 回写 README、展站计划、展站 PRD、数据库设计与开发指南
- [x] 新建并回写 `docs/guides/DEPLOYMENT.md`，明确真实部署尚未验收
- [x] 新建并回写 `deploy/README.md`，明确真实部署尚未验收
- [x] 回写金源 README 和金源 PRD 的素材状态与占位批准事实
- [x] 在 `spec.md` 记录测试、构建、人工验收和已知阻塞
- [ ] 验收完成后先同步所有长期文档，再将本目录移入 `.doc/specs/archive/phase-2-assets-and-preview/`
