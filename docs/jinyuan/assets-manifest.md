# 金源官网素材清单

> 状态日期：2026-07-16。当前未收到或核验任何客户真实素材，不得标记为真实素材或已交付。内部本地/Mock 预览可使用下述明确批准的中性占位方案；批准人 `Pan`，批准时间 `2026-07-16T20:59:07+08:00`。该批准不授权虚构客户商标、产品型号、认证、证书、目录、二维码账号或厂区事实。未来用于真实云预览的二进制文件存放于受控 OSS，不提交到代码仓库。

## Logo

- 用途：页头、首页品牌区。
- 配置位置：`brand.logoAssetId`。
- 预留 ID：`asset_logo_01`（仅供草稿，不代表真实 Asset）。
- 要求：SVG、PNG 或 WebP，≤ 2 MB；需确认商标使用许可和最终版本。
- 当前状态：客户真实 Logo 待确认；内部本地/Mock 预览占位方案已批准并用于验收，真实云启用时仍须上传 OSS 并由 Asset 完成接口复核。
- 占位策略：使用明确写有“示例 Logo / 非正式品牌素材”的非商标 SVG；本地验收 fixture 为受版本控制的 `apps/api/fixtures/jinyuan-example-logo.svg`。
- 占位批准：`Pan`，`2026-07-16T20:59:07+08:00`；上传时登记 `sourceKind=placeholder`，并写入相同批准信息。

## 产品图

- 用途：低压电流互感器分类下 `LMK1(BH)-0.66` 产品展示。
- 配置位置：`products.categories[lv-current].series[lmk1-bh].imageAssetId`。
- Asset ID：尚未分配。
- 要求：JPEG、PNG 或 WebP，≤ 5 MB；需确认图片对应型号和展示许可。
- 当前状态：客户真实产品图待确认；内部预览中性示意图方案已批准，尚未生成或登记 Asset。
- 占位策略：使用明确标注“产品示意图 / 非真实型号”的中性工业器材图，不得暗示具体型号、认证或性能。
- 占位批准：`Pan`，`2026-07-16T20:59:07+08:00`；生成后须单独上传、复核并记录 Asset ID。

## 证书图

- 用途：资质认证页。
- 配置位置：`assets.certificates[]`、`certifications.groups[].items[]`。
- Asset ID：尚未分配。
- 要求：JPEG、PNG 或 WebP，单张 ≤ 5 MB、最多 30 张；需核对证书名称、有效期和展示许可。
- 当前状态：客户证书原件待确认；内部预览的“资质文件待补充”静态卡片方案已批准，不创建证书 Asset。
- 占位策略：仅显示“资质文件待补充”卡片，不制作或展示伪造证书。
- 占位批准：`Pan`，`2026-07-16T20:59:07+08:00`。

## 产品 PDF

- 用途：全站产品样本下载。
- 配置位置：`assets.pdfCatalogAssetId`。
- Asset ID：尚未分配。
- 要求：PDF，≤ 50 MB；需确认可公开分发，并验证文件可读取。
- 当前状态：客户可公开 PDF 待确认；内部预览隐藏下载入口方案已批准，不创建 PDF Asset。
- 占位策略：隐藏下载入口，不生成虚构产品目录。
- 占位批准：`Pan`，`2026-07-16T20:59:07+08:00`。

## 微信二维码

- 用途：联系页微信入口。
- 配置位置：`contact.wechatQrAssetId`。
- Asset ID：尚未分配。
- 要求：JPEG、PNG 或 WebP，≤ 2 MB；复核人须实际扫码确认目标账号。
- 当前状态：客户二维码待确认；内部预览的“联系信息待确认”静态卡片方案已批准，不创建二维码 Asset。
- 占位策略：显示“联系信息待确认”，不得生成指向未经授权账号的二维码。
- 占位批准：`Pan`，`2026-07-16T20:59:07+08:00`。

## 厂房图

- 用途：关于我们页厂房展示。
- 配置位置：`about.factoryImageAssetIds[]`。
- Asset ID：尚未分配。
- 要求：JPEG、PNG 或 WebP，单张 ≤ 5 MB、最多 10 张；需核验拍摄场所、真实性和展示许可。
- 当前状态：客户厂房图待确认；内部预览隐藏厂房图片区块方案已批准，不创建厂房 Asset。
- 占位策略：隐藏厂房图片区块；如以后改用中性工业场景图，每张图片须重新单独审批。
- 占位批准：`Pan`，`2026-07-16T20:59:07+08:00`。

## 复核记录要求

每个登记后的 Asset 必须保留：`assetId`、用途、类型、来源、原文件名、OSS 对象键、SHA-256、大小、MIME、创建人、复核人、复核时间；占位素材还必须保留批准人和批准时间。
