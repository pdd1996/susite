# 复制为 deploy/.env 后再部署。密码仅使用 prepare-env.sh 生成的十六进制字符，
# 避免 DATABASE_URL 中出现必须 URL 编码的保留字符。
PUBLIC_IP=118.196.82.13
ADMIN_ORIGIN=http://118.196.82.13
HTTP_PORT=80
DEV_ACTOR_ID=server-operator
DB_PASSWORD=replace-with-generated-hex
MYSQL_ROOT_PASSWORD=replace-with-generated-hex
UPLOAD_TOKEN_SECRET=replace-with-at-least-32-random-characters
